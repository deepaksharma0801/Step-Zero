{
  "name": "step-zero",
  "version": "1.0.0",
  "private": true,
  "description": "Step Zero focus sprint app with a Gemini-backed planning server.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js"
  },
  "engines": {
    "node": ">=20"
  }
}
